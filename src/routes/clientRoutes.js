import Dashboard from "../pages/client/Dashboard"
import { Routes } from "./routes"

const clientRoutes = [
    { exact: true, path: Routes.client, component: Dashboard },
];

export default clientRoutes
